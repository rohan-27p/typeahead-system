{
  "name": "search-typeahead-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HLD assignment demo: search typeahead with distributed cache, trending ranking, and batch writes.",
  "scripts": {
    "seed": "node scripts/generate-dataset.js && node scripts/load-dataset.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "perf": "node scripts/performance-report.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
